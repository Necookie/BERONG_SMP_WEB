---
// Navbar.astro – Fixed top nav; imports MobileMenu React island for the hamburger toggle
import { Image } from 'astro:assets';
import MobileMenu from '../react/MobileMenu';
import ThemeToggle from './ThemeToggle.astro';
import logo from '../../assets/images/berong-elearning-logo.png';

const isCommandsPage = Astro.url.pathname.startsWith('/commands');

const navLinks = [
  { key: 'home',         href: isCommandsPage ? '/' : '#',        label: 'Home'     },
  { key: 'about',        href: isCommandsPage ? '/#about' : '#about',   label: 'About'    },
  { key: 'features',     href: isCommandsPage ? '/#features' : '#features',label: 'Features' },
  { key: 'gallery',      href: isCommandsPage ? '/#gallery' : '#gallery', label: 'Gallery'  },
  { key: 'rules',        href: isCommandsPage ? '/#rules' : '#rules',   label: 'Rules'    },
  { key: 'instructions', href: isCommandsPage ? '/#instructions' : '#instructions', label: 'Instructions' },
  { key: 'commands',     href: '/commands', label: 'Commands' },
];
---

<nav
  class="bg-surface-deep fixed top-0 w-full z-50 glass-panel"
  aria-label="Primary navigation"
>
  <div class="flex justify-between items-center px-margin-mobile md:px-margin-desktop py-2.5 max-w-container-max mx-auto">
    <!-- Logo -->
    <a href="/" aria-label="BERONG SMP home" class="flex items-center gap-2">
      <Image
        src={logo}
        alt="BERONG SMP logo"
        class="h-8 w-8 md:h-9 md:w-9 object-contain"
        width={48}
        height={48}
        loading="eager"
        format="webp"
        quality={85}
      />
      <span class="font-headline text-base md:text-lg font-bold text-white uppercase tracking-wider">
        Berong <span class="text-primary">SMP</span>
      </span>
    </a>

    <!-- Desktop links -->
    <ul class="hidden md:flex items-center gap-6 list-none" role="list">
      {navLinks.map(({ key, href, label }) => (
        <li>
          <a
            href={href}
            data-nav-key={key}
            class={[
              'font-mono text-xs font-medium tracking-widest uppercase transition-colors',
              (isCommandsPage ? label === 'Commands' : label === 'Home')
                ? 'text-primary border-b-2 border-primary pb-1'
                : 'text-on-surface-variant hover:text-on-surface hover:bg-surface-bright/10 px-2 py-1',
            ].join(' ')}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>

    <!-- Desktop CTA -->
    <div class="flex items-center gap-2">
      <a
        href="#enroll"
        class="mc-button magnetic px-5 py-1.5 font-mono text-xs font-medium uppercase tracking-wider hidden md:block"
      >
        Join Server
      </a>

      <!-- Theme toggle -->
      <ThemeToggle />

      <!-- Mobile menu island — only loads JS when scrolled into view -->
      <MobileMenu links={navLinks} client:visible />
    </div>
  </div>
</nav>
