{
  "name": "berong-smp-web",
  "private": true,
  "version": "0.0.1",
  "description": "BERONG SMP monorepo — landing site + authenticated dashboard",
  "scripts": {
    "dev:landing":   "pnpm --filter landing dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build:landing":   "pnpm --filter landing build",
    "build:dashboard": "pnpm --filter dashboard build",
    "build":  "pnpm --filter landing build && pnpm --filter dashboard build",
    "deploy:landing": "pnpm --filter landing deploy",
    "check":  "pnpm --filter landing check && pnpm --filter dashboard check",
    "typecheck": "pnpm --filter landing typecheck && pnpm --filter dashboard typecheck"
  },
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=9"
  }
}
